import { RegistrationPipe } from './registration.pipe';

describe('RegistrationPipe', () => {
  it('create an instance', () => {
    const pipe = new RegistrationPipe();
    expect(pipe).toBeTruthy();
  });
});
